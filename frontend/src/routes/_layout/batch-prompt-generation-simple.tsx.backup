import { Box, Container, Heading, VStack, Button, Text, Input, Textarea } from "@chakra-ui/react"
import { createFileRoute } from "@tanstack/react-router"
import { useState } from "react"

export const Route = createFileRoute("/_layout/batch-prompt-generation-simple")({
  component: BatchPromptGenerationSimple,
})

function BatchPromptGenerationSimple() {
  const [taskName, setTaskName] = useState("")
  const [taskDescription, setTaskDescription] = useState("")

  return (
    <Container maxW="4xl" p={6}>
      <VStack gap={8} align="stretch">
        <Heading size="lg" textAlign="center" color="gray.800">
          🚀 创建批量生成提示词任务
        </Heading>

        <Box
          bg="white"
          borderWidth="1px"
          borderColor="gray.200"
          borderRadius="lg"
          p={8}
          shadow="sm"
        >
          <VStack gap={6} align="stretch">
            <Text fontSize="20px" fontWeight="700" color="gray.700">
              📝 任务基础信息
            </Text>

            {/* 任务名称 */}
            <Box>
              <Text fontSize="16px" fontWeight="600" color="gray.600" mb={2}>
                任务名称
              </Text>
              <Input
                value={taskName}
                onChange={(e) => setTaskName(e.target.value)}
                placeholder="例如：可莉和胡桃角色提示词批量生成"
                size="lg"
                borderColor="gray.300"
              />
              <Text fontSize="12px" color="gray.500" mt={1}>
                ↳ 例如: "可莉和胡桃角色提示词批量生成"
              </Text>
            </Box>

            {/* 任务描述 */}
            <Box>
              <Text fontSize="16px" fontWeight="600" color="gray.600" mb={2}>
                任务描述
              </Text>
              <Textarea
                value={taskDescription}
                onChange={(e) => setTaskDescription(e.target.value)}
                placeholder="可选，描述此次批量生成的目的..."
                rows={4}
                resize="vertical"
                borderColor="gray.300"
              />
              <Text fontSize="12px" color="gray.500" mt={1}>
                ↳ 可选，描述此次批量生成的目的
              </Text>
            </Box>

            {/* 生成策略 */}
            <Box>
              <Text fontSize="16px" fontWeight="600" color="gray.600" mb={2}>
                生成策略
              </Text>
              <select
                style={{
                  width: "100%",
                  padding: "12px 16px",
                  borderRadius: "8px",
                  border: "1px solid #CBD5E0",
                  fontSize: "16px",
                  backgroundColor: "white",
                  cursor: "pointer",
                }}
              >
                <option value="ai">智能生成 (AI自动生成)</option>
                <option value="template">模板填充 (基于现有模板)</option>
                <option value="hybrid">混合模式 (AI+模板)</option>
              </select>
              <Text fontSize="12px" color="gray.500" mt={2}>
                智能生成: AI根据角色信息自动创作，创意性强但可能不够稳定
              </Text>
            </Box>

            {/* 提示信息 */}
            <Box p={4} bg="blue.50" borderRadius="md" borderLeft="4px solid" borderColor="blue.400">
              <Text fontWeight="600" color="blue.600" mb={1}>
                📋 功能说明
              </Text>
              <Text fontSize="14px" color="gray.600">
                此页面为批量生成功能的简化版本。完整版本包含4步骤向导、多角色选择、模板条目管理等功能，但需要解决Chakra UI v3兼容性问题。
              </Text>
            </Box>

            {/* 操作按钮 */}
            <Button
              colorScheme="blue"
              size="lg"
              px={8}
              disabled={!taskName}
              onClick={() => {
                alert(`任务创建成功！\n任务名称: ${taskName}\n描述: ${taskDescription || '无'}`)
              }}
            >
              创建任务 →
            </Button>
          </VStack>
        </Box>

        {/* 开发说明 */}
        <Box p={4} bg="yellow.50" borderRadius="md" borderLeft="4px solid" borderColor="yellow.400">
          <Text fontWeight="600" color="yellow.600" mb={2}>
            ⚠️ 开发说明
          </Text>
          <VStack gap={2} align="start">
            <Text fontSize="14px" color="gray.700">
              • 当前版本为简化版本，解决了TypeScript编译错误
            </Text>
            <Text fontSize="14px" color="gray.700">
              • 完整版本包含步骤导航、角色选择、模板管理等功能
            </Text>
            <Text fontSize="14px" color="gray.700">
              • 需要解决Chakra UI v3的API兼容性问题（Card、Alert、Select等组件）
            </Text>
            <Text fontSize="14px" color="gray.700">
              • 建议升级到项目兼容的UI组件库或使用自定义包装器
            </Text>
          </VStack>
        </Box>
      </VStack>
    </Container>
  )
}